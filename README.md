# Deep-Learning


## How to run
1. Open in Google Colab OR locally with Jupyter
2. Install dependencies:
   pip install torch torchvision matplotlib
3. Upload the file object_detection_utils.py

## Dataset
The dataset is automatically downloaded using:
git clone https://git.wur.nl/abe-datasets/education/fruit-detection-challenge.git
